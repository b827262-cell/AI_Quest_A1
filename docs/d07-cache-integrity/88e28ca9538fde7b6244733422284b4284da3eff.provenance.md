# D-07 build provenance — frozen archive of `88e28ca9538fde7b6244733422284b4284da3eff`

**CURRENT authoritative handoff record.** Supersedes
`af22d5ad….provenance.md` (same directory), whose source SHA no longer matches
the consent-handoff behaviour this lane ships.

Reviewer contract, unchanged from the previous record: the archive below holds
the exact bytes of **one** build. Verify by extracting it. **A rebuild will not
reproduce `PACKAGE_SHA`** — see §Reproducibility.

## Gate-D finding closed by this build

Codex re-ran Gate-D against `7b5e04f` and failed it on an executable source
blocker: after the learner pressed 「同意並前往 Google AI」, `page.tsx` called
`window.location.assign(url)`, so the consent action left the student page in the
same tab — against the Owner contract 「點擊同意後開 ONE 新分頁、保留原頁」.

`88e28ca` replaces that with `openConsentHandoffTab()`:

* one `window.open(url, "_blank")` per consent dialog, opener reference severed;
* called **without** a features argument on purpose — `noopener` in the third
  argument makes `window.open` return `null` even on success, which would read as
  a blocked popup and double-fire the fallback;
* blocked popup → a visible manual `target="_blank" rel="noopener noreferrer"`
  link, never a navigation (`openConsentHandoffTab` returns `"blocked"`);
* the one-shot budget guard lives in a `useRef` created when the dialog opens, so
  a repeated press returns `"duplicate"` and opens nothing;
* no auto-submit, no re-submit, no fetch/relay/scrape — the question only ever
  travels inside the Google URL, exactly as `buildGoogleAiModeUrl` builds it.

Regression coverage: 9 new cases in `site/tests/auto-fallback.test.mjs`
(one press → exactly one open; URL params `q`/`udm=50`/`aep=11`/`hl=zh-TW` and no
extra query keys; no features argument; duplicate press opens nothing; fresh
dialog gets its own budget; blocked popup and throwing `open()` never navigate;
missing `window` degrades to blocked; and a `page.tsx` source gate proving the
consent path routes through the helper and contains no
`location.assign|replace|href`, `requestSubmit`, `fetch` or `XMLHttpRequest`).
The source gate was negative-controlled against pre-fix `page.tsx`: it fails
there (`not ok 27`) and passes at `88e28ca`.

## Frozen build identity

| Field | Value |
| --- | --- |
| `SOURCE_SITE_SHA` | `88e28ca9538fde7b6244733422284b4284da3eff` |
| Lane branch | `agent/d07-cache-integrity-qwen-e500` |
| Lane BASE | `29005f4afaa85765f6cc374a4aa91df58f92b09c` |
| D-07 chain | `d1d80c3` → `e7fd3da` → `af22d5a` → `a5e1de6`/`ad63045`/`7b5e04f` (docs) → **`88e28ca` (Gate-D fix)** |
| Tip at build time | `88e28ca…` (built from the committed tree; working tree clean, `git status --porcelain` empty) |
| Build command | `rm -rf dist && npm test` → `vinext build` + `verify-upload-package-size.mjs dist` + 173 tests |
| Runtime | node v22.23.2, npm 10.9.8, linux/x64, host `b827262-E500-G9-WS760T`, CI=false |
| Build instance | 2026-09-24T09:11:16Z (max `dist` mtime), frozen 38 s later at 09:11:54Z, before any other `dist` write |
| Upload root | `site/dist` |
| Files / bytes | 99 / 2,712,729 |
| Largest file | 549,044 B (`client/_next/static/chunks/transformers-BwfJg0-o.js`), limit 25,000,000 B |
| Writer | Qoder-E500 (single writer; Qwen-E500 standby) |

## Archive

| Field | Value |
| --- | --- |
| Path | `release-artifacts/d07-cache-integrity/88e28ca9538fde7b6244733422284b4284da3eff.dist-19af6e7a.tar` |
| SHA-256 | `b4c94ca450f6c0a06025ff1a6e5d26e18b500432178e8e1a01c51495594dd753` |
| Bytes | 2,805,760 (99 file entries + 19 directory entries, all under `dist/`) |
| Tracked manifest | `docs/d07-cache-integrity/88e28ca….frozen-manifest-19af6e7a.txt` (99 rows) |
| Sanitized metadata | `…dist-19af6e7a.metadata.json`, sha256 `8fa096c160f4e779677eea487e577465fd5d91a13987b2227e7c0d9ee8c2c3b7`, written via `scripts/release-artifacts.mjs` → `{"written":true,"leakage":"PASS"}` |

```bash
tar --format=gnu --sort=name --mtime='2026-09-24 09:11:16 UTC' \
    --owner=0 --group=0 --numeric-owner \
    -C site -cf release-artifacts/d07-cache-integrity/88e28ca9538fde7b6244733422284b4284da3eff.dist-19af6e7a.tar dist
```

Tar normalization touches only tar metadata; member bytes are stored
uncompressed, so content digests are unaffected by re-packing choices.

**Persistence caveat (unchanged).** `release-artifacts/` is git-ignored
(`.gitignore:24`), so the pushed branch carries SHA/bytes/path + manifest +
provenance, while the archive **bytes live on this lane host only**. Cross-host
durability needs the release owner to stage the tar into the project artifact
store.

## Digests — this build instance only

```
PACKAGE_SHA    19af6e7a70a98c81d770eb72a4fb212c4fc70f8e6679af3c49ba79c5df279238
MANIFEST_SHA   ef984b5372c827a9bef1c7e5fca5850acd399afdb8e28c8e500968d7ecca0913
ROW_SET_DIGEST 50e126d4ab4aed85e2fa0c004d91fdc328d87307a8a4033041183a60e49dc80a
ARCHIVE_SHA256 b4c94ca450f6c0a06025ff1a6e5d26e18b500432178e8e1a01c51495594dd753
```

Definitions and the derivation tool (`derive-package-sha.mjs`, same directory)
are documented in `af22d5ad….provenance.md`; in short `PACKAGE_SHA` is the
`verify-upload-package-size.mjs` digest over sorted
`path\0bytes\0sha256(file)\n`, `MANIFEST_SHA` is the sha256 of the tracked
manifest, and `ROW_SET_DIGEST` is the sha256 of the path-sorted
`[[path, bytes, sha256], …]` JSON (redundant, kept because the metadata JSON
carries it).

## Verify the frozen bytes

```bash
AF=88e28ca9538fde7b6244733422284b4284da3eff
sha256sum release-artifacts/d07-cache-integrity/$AF.dist-19af6e7a.tar
rm -rf /tmp/d07-verify && mkdir -p /tmp/d07-verify
tar -xf release-artifacts/d07-cache-integrity/$AF.dist-19af6e7a.tar -C /tmp/d07-verify
cd site
node scripts/verify-upload-package-size.mjs /tmp/d07-verify/dist
node ../docs/d07-cache-integrity/derive-package-sha.mjs \
     ../docs/d07-cache-integrity/$AF.frozen-manifest-19af6e7a.txt
```

Measured in this lane at 2026-09-24T09:15:06Z with exactly those commands:
archive sha256 `b4c94ca4…`; extracted tree → `PACKAGE_SHA=19af6e7a…`,
99 files / 2,712,729 bytes, `MAX_FILE_BYTES=549044`, `OVERSIZE_FILES=none`,
`FILE_SIZE_GATE=PASS`; the manifest rebuilt from the extracted tree is
byte-identical to the tracked manifest, and `derive-package-sha.mjs` on that
manifest returns the same `19af6e7a…`.

## Reproducibility: NOT reproducible

Six builds of byte-identical `site/` in this lane produced six different
`PACKAGE_SHA` values; two have retained evidence, and the two frozen archives
(`499fc11e…` for `af22d5a`, `b4c94ca4…` for `88e28ca`) each re-hash to their own
recorded digest. Root cause and consequences are written up in
`af22d5ad….provenance.md` (Vite/Rollup chunk-name rotation; `dist/` carries no git
SHA). This lane changes no build tooling and does not attempt a deterministic-Vite
fix — freezing one instance is the sanctioned workaround, per the Owner.

Comparing the two frozen archives' manifests (different source, so this is *not*
a pure nondeterminism sample, but it shows what the rotation costs):

| Observation | Value |
| --- | --- |
| `af22d5a` / `8fd53757` totals | 99 files / 2,712,148 B |
| `88e28ca` / `19af6e7a` totals | 99 files / 2,712,729 B |
| paths shared by both builds | 80 |
| of those, byte-identical | 68 |
| of those, same length but different content | 12 (embedded hash names swap at equal length) |
| paths unique to one build (name rotation) | 19 old-only + 19 new-only |
| where the +581 B actually went | `client/…/page-*.js` +291 B and `server/ssr/…/page-*.js` +290 B — the whole delta, and the only size change in the package |

So the consent fix costs 581 packaged bytes, and 19 further chunk names rotated
with no size change at all. Expect a fresh `npm run build` of `88e28ca` to produce
a **different** `PACKAGE_SHA` from a different name set; compare
`TOTAL_BYTES=2,712,729`, `MAX_FILE_BYTES=549044` and the manifest against this
archive instead.

## Gate results recorded against `88e28ca`

| Gate | Result |
| --- | --- |
| `git diff 29005f4..HEAD --check` | PASS (exit 0) |
| Focused consent regression (`auto-fallback`) | 27/27 PASS (18 pre-existing + 9 new) |
| Focused cache/bootstrap | 28/28 PASS |
| Full `npm test` (same run that produced the frozen `dist`) | 173 tests — 171 PASS, 2 FAIL = `environment-alignment` F6/F7 only |
| Typecheck | changed files clean; `tsconfig.auto.json` exit 0; 28 repo-wide errors, none in `app/page.tsx`, `app/auto-fallback.ts` or `app/local-model-*.ts` |
| Lint (project eslint, D-07 + Auto files) | PASS (exit 0); repo-wide 36 pre-existing `no-explicit-any` errors in 12 non-Auto files |
| Build + `package:size-gate` | PASS, `OVERSIZE_FILES=none` |
| Archive extract → recompute → manifest diff | PASS (identical, recorded above) |
| Secrets / fixture safety / production isolation | PASS (0 secrets) / PASS (10/10) / PASS |
| Bindings (`verify-environment-alignment`) | 14/15 PASS — `CHECK_captured_at` FAIL (stale SOT) |

`captured_at` stays **fail closed**: the staging source-of-truth is still stamped
`2026-09-22T07:16:40.000Z`, older than the 24 h rule. No timestamp was edited,
`maxAgeHours` was not raised, no gate logic was weakened; re-capture belongs to
the release owner. `PREDEPLOY_CONFIG_PASS`, `POSTDEPLOY_LIVE_PENDING` are
reported unchanged.
